{
  "name": "planner-2026",
  "version": "1.0.0",
  "description": "Digital Planner 2026 - React Application",
  "private": true,
  "dependencies": {
    "lucide-react": "^0.263.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@vitejs/plugin-legacy": "^7.2.1",
    "@vitejs/plugin-react": "^4.0.0",
    "terser": "^5.46.0",
    "vite": "^7.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:static": "STATIC_BUILD=true vite build",
    "preview": "vite preview",
    "preview:static": "vite preview --outDir dist-static --port 3001",
    "serve:static": "vite preview --outDir dist-static --port 3001 --open",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:static": "playwright test tests/static-build.spec.js"
  }
}
